const superHeros: string[] = []
// const heroPower: number[] = []
const heroPower: Array<number> = []


type User = {
    name: string;
    isActive: boolean;
}

const allUsers: User[] = [] //User type array

const MLModels: number[][] = [
    [255, 255, 255],
    []
]

superHeros.push("spider")
heroPower.push(2)

allUsers.push({
    name:"",
    isActive:true,
}) // pushing element in array of User type